# Socket.IO Чат

Простой чат-приложение на **Vanilla JavaScript** + **Socket.IO** + **SASS** с ESLint проверкой.

## 🚀 Особенности

- **Без фреймворков** — чистый JavaScript (никакого React)
- **Реал-тайм сообщения** — Socket.IO WebSocket
- **SASS стили** — модульная стилизация
- **ESLint** — проверка кода на ошибки
- **Автоскролл** — новые сообщения видны сразу
- **История сообщений** — сохраняется на сервере

## 📦 Быстрый старт

```bash
npm install
npm run dev
```

Откройте http://localhost:5173

## 🏃 Команды

- `npm run dev` — запустить клиент + сервер одновременно
- `npm run dev:client` — только клиент (Vite на порту 5173)
- `npm run dev:server` — только сервер (Socket.IO на порту 3001)
- `npm run lint` — проверить код через ESLint
- `npm run build` — собрать фронтенд
- `npm start` — запустить только сервер

## 📁 Структура проекта

```
src/
  ├── main.js                # Клиент: подключение сокета и логика чата
  ├── styles/
  │   ├── index.scss         # Глобальные стили, переменные, тема
  │   └── App.scss           # Стили компонентов чата
  └── assets/
server/
  └── index.js               # Node.js сервер с Socket.IO
index.html                   # HTML шаблон
package.json                 # Зависимости и скрипты
eslint.config.js             # Конфиг ESLint
vite.config.js               # Конфиг Vite
```

## 🔌 Как работает

**Клиент** (`src/main.js`):
- Подключается к серверу через Socket.IO
- Отправляет сообщения: `socket.emit('chat:message', { user, text })`
- Получает события: `chat:history` (история) и `chat:message` (новое сообщение)
- Отображает все в HTML с автоскроллом

**Сервер** (`server/index.js`):
- Слушает порт 3001
- Хранит сообщения в памяти
- Отправляет историю при подключении клиента
- Трансляет новые сообщения всем подключенным

## 🎨 Стили (SASS)

Используются CSS переменные для кастомизации:
- `--text-primary`, `--text-secondary` — цвета текста
- `--accent`, `--accent-2` — цвета акцентов
- `--panel`, `--border`, `--shadow` — стили панелей

Измените их в `src/styles/index.scss`.

## 🧹 ESLint

Проверяет код на ошибки. Правила настраиваются в `eslint.config.js`.

```bash
npm run lint
```

## 🌐 Переменная окружения

Измените адрес сервера через `VITE_SOCKET_URL`:

```
VITE_SOCKET_URL=http://example.com:3001
```

По умолчанию: `http://localhost:3001`

- Стили в `src/styles/*.scss`, шрифты и базовые переменные подключены в `index.scss`.
